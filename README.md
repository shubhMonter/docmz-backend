## Available Scripts

In the project directory, you can run:

### `npm start`


## Setup

### `npm install`

Add a `.env` file at the directory root

linux `touch .env`
windows `notepad .env`

## Example

MONGO_URI=myuri
SECRETKEY=mysecret
